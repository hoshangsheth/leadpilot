# LeadPilot

AI WhatsApp lead qualifier — see `System Blueprint & Documents/` for the full architecture (`whatsapp-lead-qualifier-blueprint.md`) and the lean v1 build order (`v1-leadpilot-blueprint.md`) currently being followed.

Full README (demo, stack badges, architecture diagram, setup) gets written after v1 is live — see the v1 blueprint's "After it's live" section.
